export default {
  // 指定换行最大长度
  printWidth: 120,
  // 指定使用单引号
  singleQuote: true,
  // 指定使用尾随逗号
  trailingComma: 'none',
  // 指定使用箭头函数
  arrowParens: 'avoid',
  // 指定使用tab缩进
  tabWidth: 2,
  // 指定使用tab缩进
  useTabs: false,
  // 结尾使用分号
  semi: true,
  // 指定使用空格
  bracketSpacing: true,
  // 将多行HTML元素的闭合标签放在同一行
  bracketSameLine: false,
  // 指定使用范围
  rangeStart: 0,
  // 指定使用范围
  rangeEnd: Infinity
};
